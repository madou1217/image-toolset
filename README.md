# 🖼️ Image Toolset — 图片工具集

一套精美的在线图片处理工具，基于 Canvas API 构建，支持多种图片操作。

## ✨ 功能列表

### 📐 图片拼接工具
- 可视化拖拽操作
- 支持左右拼接 & 上下拼接
- 边缘变形与等比缩放
- 边缘吸附对齐
- 撤销系统（Ctrl/⌘+Z）
- 拼接后交换位置
- 导出 PNG

## 🚀 快速开始

```bash
# 启动开发服务器
npm run dev

# 或直接使用
npx -y live-server --port=8080 .
```

然后在浏览器打开 `http://localhost:8080`

## 📁 项目结构

```
image-toolset/
├── index.html              # 工具集首页 / 导航
├── css/
│   ├── common.css          # 全局样式 & 设计tokens
│   └── stitch.css          # 拼接工具专用样式
├── js/
│   └── stitch/
│       ├── app.js          # 主入口
│       ├── state.js        # 状态管理
│       ├── ImageObj.js     # 图片对象类
│       ├── canvas.js       # Canvas 渲染
│       ├── deform.js       # 变形渲染
│       ├── interaction.js  # 交互事件
│       ├── stitcher.js     # 拼接逻辑
│       ├── exporter.js     # PNG 导出
│       ├── undo.js         # 撤销系统
│       └── utils.js        # 工具函数
├── tools/
│   └── stitch.html         # 拼接工具页面
├── package.json
└── README.md
```

## 🛠️ 技术栈

- 原生 HTML / CSS / JavaScript
- ES Modules
- Canvas 2D API
- Google Fonts (Inter)

## 📝 License

MIT
